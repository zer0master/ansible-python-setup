---
- name: install pip3
  ansible.builtin.apt:
    update_cache: yes
    package:
      - python-pip3
    state: present
  tags:
    - pythonpkgs

- name: add python3 packages
  ansible.builtin.pip:
    state: present
    name: "{{ pypi_modules }}"
    umask: 0022
